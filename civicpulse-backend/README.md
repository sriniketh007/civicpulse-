# CivicPulse AI Backend

CivicPulse AI aggregates and categorizes citizen feedback using Gemini 2.5 Flash.

## Setup
1. `python -m venv venv`
2. `venv\Scripts\activate`
3. `pip install -r requirements.txt`
4. Create `.env` file with `GEMINI_API_KEY=...`
5. Run server: `python main.py`